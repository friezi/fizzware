################################################################################
########################### FOR EDITING ########################################

TARGET = llparser

# change if modules are in a deeper directory
ROOT_DIR = ../../

MAIN_MODULE = main
LOCAL_MODULES =
EXTERN_MODULES =
EM_PATH =

TT = $(ROOT_DIR)/fztooltempl
ST = $(ROOT_DIR)/fzsystools

# library-sources
LSRC = src

#  alternativ o. zusaetzlich Pfad fuer lokale Klassenbibliotheken (statisch)
CLASSLIBRARY_PATH = $(ROOT_DIR)/lib
CLASSLIBRARIES = fztooltempl
CLASS_MODULES_SRCS = $(TT)/$(LSRC)
CLASS_MODULES_PATHS = $(TT)
CLASS_MODULES = $(TT)/$(LSRC)/exception $(TT)/$(LSRC)/utils $(TT)/$(LSRC)/datastructures $(TT)/$(LSRC)/llparser

IMPORTANT_HEADERS = $(TT)/$(LSRC)/graph

LIBRARY_INCLUDE_PATHS =
LIBRARIES =
LIBRARY_PATHS =

ADDITIONAL_DISTFILES =

# in case that ansi is not allowed
# NOANSI = true

### check the field CC ########################################################
################################################################################
################################################################################
