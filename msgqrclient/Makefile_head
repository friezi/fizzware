################################################################################
########################### FOR EDITING ########################################

TARGET = msgqrclient

MAIN_MODULE = main
LOCAL_MODULES =
EXTERN_MODULES =
EM_PATH =

TT = ../fztooltempl

CLASSLIBRARY_PATH = ../lib
CLASSLIBRARIES = fztooltempl
CLASS_MODULES_PATHS = $(TT)
CLASS_MODULES = $(TT)/exception $(TT)/tcpclient $(TT)/msgqueueregclient $(TT)/socket

IMPORTANT_HEADERS =

ifeq ($(UNIX),true)
	NETWORKLIBS = socket nsl resolv
else
	NETWORKLIBS =
endif

LIBRARY_INCLUDE_PATHS =
LIBRARIES = $(NETWORKLIBS)
LIBRARY_PATHS = /usr/lib

ADDITIONAL_DISTFILES =

# in case that ansi is not allowed
# NOANSI = true

### check the field CC ########################################################
################################################################################
################################################################################
