################################################################################
########################### FOR EDITING ########################################

TARGET = msgqrclient

MAIN_MODULE = main
LOCAL_MODULES =
EXTERN_MODULES =
EM_PATH =

TT = ../fztooltempl
ST = ../fzsystools

# library-sources
LSRC = src

CLASSLIBRARY_PATH = ../lib
CLASSLIBRARIES = fztooltempl fzsystools
CLASS_MODULES_SRCS = $(TT)/$(LSRC) $(ST)/$(LSRC)
CLASS_MODULES_PATHS = $(TT) $(ST)
CLASS_MODULES = $(TT)/$(LSRC)/exception $(ST)/$(LSRC)/tcpclient $(ST)/$(LSRC)/msgqueueregclient $(ST)/$(LSRC)/socket

IMPORTANT_HEADERS =

ifeq ($(UNIX),true)
	NETWORKLIBS = socket nsl resolv
else
	NETWORKLIBS =
endif

LIBRARY_INCLUDE_PATHS =
LIBRARIES = $(NETWORKLIBS)
LIBRARY_PATHS = /usr/lib

ADDITIONAL_DISTFILES =

# in case that ansi is not allowed
# NOANSI = true

### check the field CC ########################################################
################################################################################
################################################################################
