################################################################################
########################### FOR EDITING ########################################

TARGET = msgqrd

MAIN_MODULE = main
LOCAL_MODULES = msgqueueregdaemon scanner msgqueuelist
EXTERN_MODULES =
EM_PATH =

TT = ../fztooltempl
ST = ../fzsystools

# library-sources
LSRC = src

CLASSLIBRARY_PATH = ../lib
CLASSLIBRARIES = fztooltempl fzsystools
CLASS_MODULES_SRCS = $(TT)/$(LSRC) $(ST)/$(LSRC)
CLASS_MODULES_PATHS = $(TT) $(ST)
CLASS_MODULES = $(TT)/$(LSRC)/exception $(ST)/$(LSRC)/socket $(ST)/$(LSRC)/tcpserver $(ST)/$(LSRC)/signalhandler $(ST)/$(LSRC)/messagequeue $(ST)/$(LSRC)/stdmessageserver $(TT)/$(LSRC)/cmdlparser

IMPORTANT_HEADERS = $(TT)/$(LSRC)/datastructures $(TT)/$(LSRC)/functions

ifeq ($(UNIX),true)
	NETWORKLIBS = socket nsl resolv
else
	NETWORKLIBS =
endif

LIBRARY_INCLUDE_PATHS =
LIBRARIES = $(NETWORKLIBS)
LIBRARY_PATHS = /usr/lib

ADDITIONAL_DISTFILES =

# in case that ansi is not allowed
# NOANSI = true

### check the field CC ########################################################
################################################################################
################################################################################
