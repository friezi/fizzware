################################################################################
########################### FOR EDITING ########################################

TARGET = msgqrd

MAIN_MODULE = main
LOCAL_MODULES = msgqueueregdaemon scanner msgqueuelist
EXTERN_MODULES =
EM_PATH =

TT = ../fztooltempl

CLASSLIBRARY_PATH = ../lib
CLASSLIBRARIES = fztooltempl
CLASS_MODULES_PATHS = $(TT)
CLASS_MODULES = $(TT)/exception $(TT)/socket $(TT)/tcpserver $(TT)/signalhandler $(TT)/messagequeue $(TT)/stdmessageserver $(TT)/cmdlparser

IMPORTANT_HEADERS = $(TT)/datastructures $(TT)/functions

ifeq ($(UNIX),true)
	NETWORKLIBS = socket nsl resolv
else
	NETWORKLIBS =
endif

LIBRARY_INCLUDE_PATHS =
LIBRARIES = $(NETWORKLIBS)
LIBRARY_PATHS = /usr/lib

ADDITIONAL_DISTFILES =

# in case that ansi is not allowed
# NOANSI = true

### check the field CC ########################################################
################################################################################
################################################################################
